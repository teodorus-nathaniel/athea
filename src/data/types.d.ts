export interface ProjectOverviewData {
  thumbnail: string | StaticImageData
  title: string
  titleDesc: string
  subtitle: string
}

export interface ServiceData {
  image?: ImageData
  title: string
  subtitle: string
}
